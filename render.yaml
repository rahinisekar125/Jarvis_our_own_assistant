services:
  - type: web
    name: jarvis-public-demo
    runtime: python
    plan: free
    buildCommand: pip install -r requirements-public.txt
    startCommand: python -m jarvis_assistant --mode server --host 0.0.0.0 --port $PORT --public
    autoDeploy: true
